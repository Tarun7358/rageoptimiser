/**
 * M-9 CLEANUP: TOTP verification was removed from this dashboard.
 * This file is intentionally empty. If TOTP is re-implemented in the future,
 * build the modal here. The file is kept to avoid import errors in any
 * edge-case code paths that may still reference it.
 */

export {}; // prevents "module has no exports" TS error
